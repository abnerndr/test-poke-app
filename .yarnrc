# Yarn configuration to reduce disk space usage
# Only install optional dependencies that are actually needed
ignore-optional false

# Network settings
network-timeout 100000

# Cache settings
cache-folder .yarn/cache

